# Installation

```zsh
cd web
npm i
```

# AppID

Set your appId to `./web/.envrc` & `./ios/OpenLive/KeyCenter.swift` .

# Run

## Web

```zsh
cd web
npm run start
```

## iOS

```zsh
open ios/OpenLive.xcodeproj # open with xcode
command + R
```
